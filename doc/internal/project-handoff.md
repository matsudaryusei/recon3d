# 共同開発プロジェクト 引き継ぎドキュメント

> **🗄 読まなくていい文書です。**
> テーマを選んだときの検討経緯（2026-08-14 時点）を保存してあるだけです。**現在の正典は [計画書](../3d-reconstruction-project-plan.md)** で、本書と矛盾する場合は計画書が優先されます。
> 入口は **[../README.md](../README.md)** です。

**プロジェクト名（仮）**: 多視点画像とシルエットを統合した3D形状復元システム
**作成日**: 2026-08-14
**目的**: 本ドキュメントは、別セッションのClaudeが文脈を再構築するための引き継ぎ資料です。

> ## ⚠ 本書は背景資料です（2026-08-15 追記）
>
> **現在の正典は `../3d-reconstruction-project-plan.md`（開発計画書）です。** 本書と計画書が矛盾する場合、**計画書が優先**されます。本書は「なぜその結論に至ったか」の経緯を保存するための資料であり、その後の決定で更新された箇所があります。
>
> **本書の記述のうち、既に更新されたもの**
>
> | 本書の記述 | 現在の状態 | 参照 |
> |---|---|---|
> | §8「Python 3.11 を**推奨**。Phase 0 の最初に実機で確認して確定」 | **✅ 確定済み（2026-08-15）。Blender 5.0.1 / 内蔵Python 3.11.13。プロジェクトは 3.11 に固定** | 計画書 §7-2 |
> | §10「Blenderのバージョンと内蔵Pythonバージョン」（未確認事項） | **✅ 解決済み**。原本は `blender_system/system-info.txt` | 計画書 §7-6 |
> | §5 の役割分担 | 「主担当＋副担当」＋ペアプロ3か所に組み替え済み。**さらに 2026-08-15 に「副担当も一部を実装する」形へ改訂**（詳しいほうが設計を握って一緒に作る） | 計画書 §3-2・§3-6 |
> | §6「ターンテーブルの回転角が真値になる」 | **カメラ姿勢の真値であって形状の真値ではない**。合成データで補完 | 計画書 §2・§0 |
> | §7 Phase 0「実写で実測」 | **Blender合成データで着工**し、W03 に実写を1セット | 計画書 §2 |
> | §9 撤退ライン | 維持。**判定タイミングを W11 / W15 に明記** | 計画書 §12 |

---

## 0. このドキュメントの使い方

新しいセッションでは、以下を前提として会話を開始してください。

- 開発テーマは **確定済み**（§2）。再検討の提案は不要。
- 使用言語は **Python単一言語で確定**（§8）。**バージョンは 3.11 で確定済み**（計画書 §7-2）。
- 次に決めるべき項目は ~~**§10 の未決定事項リスト**~~ → **計画書 §16「未決定として残る項目」**。§10 は計画書 §0 で全て決定済み。
- 「なぜその結論になったか」の根拠は各節に記載。覆す提案をする場合は、記載された根拠に対して反論すること。

---

## 1. 前提条件

### 開発者

| | 担当者 | 専門 |
|---|---|---|
| A | 関口（本ユーザー） | 応用数理・数値線形代数 |
| B | 松田（1名） | CG・画像処理（Blender / Unity / 画像処理の経験あり） |

### 関口の背景（重要）

- 東京都市大学 情報工学部 情報科学科 3年
- 相原研究室（応用数理研究室）を第一志望として配属予定
  - 研究室のテーマ: 数値線形代数（Krylov部分空間法、反復解法）、リーマン多様体最適化、**悪条件最小二乗問題**
- 学習ロードマップ: VMLS → **T&B（Trefethen & Bau, 現在Lecture 12付近）** → **Saad（Ch.6-7, 9-10を選択的に）** → Nocedal & Wright → Boumal
- 関心の核: 「行列で表現できる世界の構造」。個別のAI技術やLLM構築には関心なし。
- 目標: 修士段階での査読付き論文・国際会議発表

### プロジェクトの二重目的

1. **就活アピール**: Git/GitHubを用いた共同開発の実績
2. **研究への助走**: 相原研のテーマ（悪条件最小二乗・反復解法）に直結する実装経験

→ この2つを同時に満たすことが、テーマ選定の最重要基準。

### 期間・工数の想定

- 期間: 3〜4か月（余裕があれば5〜6か月）
- 工数: 1人あたり週6〜10時間（1日1〜2時間 × 週3〜4日）
- 原則: 「休日に10時間、その後2週間放置」より「毎週継続」を優先（GitHubのコミット履歴が継続性を示すため）

---

## 2. 確定したテーマ

### 一言でいうと

**物をぐるっと一周撮った写真から、3Dモデルを作るシステム。**

内部で実際にやっていることは：

> 未知数のほうが式より多い（あるいは誤差で矛盾する）巨大で疎な連立方程式を、誤差が最小になるように反復法で解く

写真も3Dモデルも、その入口と出口にすぎない。

### 統合の対象

元々の候補にあった以下2案を **統合** する。

- **① 多視点画像からの3Dモデル自動生成**（SfM系: 特徴点 → カメラ姿勢 → 三角測量 → 点群）
- **④ シルエットからの物体形状復元**（Visual Hull系: 輪郭抽出 → ボクセル彫刻 → メッシュ）

### 統合の論理（このプロジェクトの存在理由）

2手法の弱点が **相補的** であることが根拠。

| 手法 | 得意 | 不得意 |
|---|---|---|
| ① 多視点復元 | テクスチャのある面、凹み部分 | 無地の面、輪郭付近、点群が疎で穴が空く |
| ④ シルエット復元 | 外形が確実に取れる、必ず閉じた形状になる | 凹んだ部分を原理的に削れない、テクスチャ情報を使わない |

**合流点は2箇所:**

1. ④の「削る」処理には撮影角度（カメラ姿勢）が必須 → ①がこれを供給する
2. ④は凹形状を削れない（お椀の内側が埋まる） → ①の点群が「ここは物体の内側ではない」と教えて補正する

→ 「2つやりました」ではなく「**片方の欠点をもう片方で補う設計にしました**」と説明できる。統合の必然性が語れる点が重要。

---

## 3. パイプライン

```
          写真 10〜20枚（ターンテーブル + 単色背景）
                        │
        ┌───────────────┴───────────────┐
        │                               │
        ▼ 【トラック A：①】             ▼ 【トラック B：④】
   特徴点検出・マッチング          背景差分・セグメンテーション
        │  ← CG担当                     │  ← CG担当
        ▼                               │
   カメラ姿勢推定（DLT）                │
   　└ 同次連立方程式 → SVD             │
        │  ← 数理担当                   │
        ▼                               │
   三角測量 → 疎な3D点群                │
   　└ 線形最小二乗                     │
        │  ← 数理担当                   │
        ▼                               │
   バンドル調整                         │
   　└ 非線形最小二乗 / LM法            │
   　└ 疎な正規方程式・Schur補元        │
        │  ← 数理担当                   │
        │                               │
        └──── 高精度カメラ行列 ────────▶│
                                        ▼
                              ボクセル空間の彫刻
                              （Space Carving）
                                 │  ← 数理担当
                                 ▼
                          Marching Cubes → メッシュ
                                 │  ← CG担当
                                 ▼
                    ┌────────────┴────────────┐
                    │  点群による凹み部分の補正  │
                    │  ラプラシアン平滑化       │
                    │  └ 疎行列連立方程式 → CG法 │
                    └────────────┬────────────┘
                                 │  ← 数理担当
                                 ▼
                        テクスチャ投影 → Blender
                                    ← CG担当
```

---

## 4. 数理的な芯（関口の担当領域）

このテーマを採用した最大の理由。**4つの計算核を意識的に自作範囲に確保している。**

### 4-1. カメラ姿勢推定 — SVDによる同次最小二乗

`A p = 0` を `||p|| = 1` の制約下で解く。解は `A` の最小特異値に対応する右特異ベクトル。
→ T&B で学んだSVDがそのまま実装対象になる。

### 4-2. 三角測量 — 線形最小二乗と条件数

**カメラ間の視差角が小さいと問題が悪条件化する。** 奥行き方向の誤差が横方向より桁で大きくなる現象を、正規方程式の条件数で説明できる。
→ 「撮影角度 vs 条件数 vs 復元誤差」がそのまま実験項目になる。

### 4-3. バンドル調整 — 悪条件な非線形最小二乗（最重要）

再投影誤差の最小化：

```
min  Σ || π(R_i X_j + t_i) - x_ij ||²
```

- Gauss-Newton の正規方程式 `JᵀJ Δ = -Jᵀr` を組むと、`JᵀJ` は疎なブロック構造を持つ
- 3D点ブロックを **Schur補元** で消去 → カメラのみの縮小系（対称正定値・疎）が残る
- **この縮小系をCG法で解く** → Saad の実装がそのまま使える
- Levenberg–Marquardt の減衰項 `λI` は形式的に **Tikhonov正則化** と同一
  → `λ` の選び方が収束性と精度を左右する構図は、相原研の悪条件最小二乗そのもの

### 4-4. メッシュ平滑化 — 疎ラプラシアン系

`(I + λL) x' = x` を解く。`L` は疎対称行列。CG法 + 前処理の効果測定の題材。

### 教科書との対応

| 工程 | 数学 | 対応教科書 |
|---|---|---|
| 姿勢推定 | SVD、同次最小二乗 | T&B Lec. 4–5, 11 |
| 三角測量 | 最小二乗、条件数 | T&B Lec. 12, 18 |
| バンドル調整 | 非線形最小二乗、GN/LM、Schur補元 | Nocedal Ch. 10 |
| 縮小系ソルバ | CG法、前処理 | Saad Ch. 6, 9–10 |
| メッシュ平滑化 | 疎SPD系、CG法 | Saad Ch. 6 |

**読書ロードマップ（T&B → Saad → Nocedal）が、そのまま開発工程表になっている。** これは意図的な設計。

---

## 5. 役割分担とインターフェース

| | 関口（応用数理） | 松田（CG・画像処理） |
|---|---|---|
| 主担当 | 姿勢推定・三角測量・バンドル調整・反復ソルバ・ボクセル彫刻 | 撮影・前処理・セグメンテーション・特徴点抽出・メッシュ後処理・テクスチャ・Blender連携・GUI |
| 出力 | カメラ行列、3D点群、収束・精度の実験データ | シルエット画像、最終メッシュ、可視化、デモ動画 |
| I/F | `cameras.json`、`points.ply` | `masks/*.png`、`mesh.obj` |

### 最重要の設計方針：インターフェースで切る

**境界をファイル形式で定義する。** これが決まっていれば、お互いの実装完了を待たずに並行作業できる。

#### ①→④ の依存関係の正確な把握

```
①-a 特徴点マッチング     … CG担当
①-b カメラ姿勢推定       … ★④が必要とするのはここだけ
①-c 三角測量 → 点群      … 凹み補正でのみ使う
①-d バンドル調整         … 精度向上。なくても④は動く

④-a シルエット抽出       … ①と完全に独立
④-b ボクセル彫刻         … ①-b の出力が必要
④-c メッシュ化           … ①と独立
```

→ **④が待っているのは `cameras.json` という1ファイルだけ。**

#### 「①を完成させてから④」は採用しない（重要）

理由:
- 松田が6週間何もできない。2人プロジェクトとして致命的。GitHub履歴も片方だけになる。
- ①-b が詰まった瞬間に全体が止まる（単一障害点）。

#### 採用する組み方：モックによる分離

**ターンテーブルの回転角からカメラ姿勢は幾何的に計算できる。** これを `cameras.json` に書き出しておけば、④は①の完成を待たずに着工できる。

```
Phase 1  cameras.json の仕様だけ確定
           │
   ┌───────┴───────┐
   ▼               ▼
 ①を実装        ④を実装
 （関口）      （松田・回転台の角度を使用）
   │               │
   └───────┬───────┘
           ▼
   ①の推定値に差し替え
```

#### 副次的な利点

| カメラ姿勢 | 得られるもの |
|---|---|
| 回転台の真値 | 手法の理論的な上限性能 |
| ①の推定値 | 実際の性能 |

差分が「**姿勢推定の誤差が最終形状にどれだけ効くか**」になる。誤差の伝播を切り分けられる、質の高い実験。

---

## 6. 撮影条件（スコープ制限）

難易度を決めるのは実装よりも入力条件。以下を固定する。

- **ターンテーブル（回転台）+ 固定カメラ**、10〜20°刻みで撮影
- **単色背景**（緑や黒の布）→ セグメンテーションが背景差分で済む
- **チェッカーボードで内部パラメータを事前較正** → 推定するのは外部パラメータのみ
- 被写体は **テクスチャのあるフィギュア・マグカップ程度**。鏡面・透明物は除外
- 照明固定

### 最大の利点

**ターンテーブルの回転角が真値（ground truth）になる。**
推定したカメラ姿勢を真値と比較できるので、「なんとなくそれっぽい形が出た」ではなく **角度誤差 [deg]** という定量評価ができる。他の候補テーマにはない強み。

---

## 7. 開発プロセス

### 方針：PoC → ウォーターフォールの折衷

純粋なウォーターフォールは不採用。理由は、このテーマでは **基本設計の妥当性が実装してみるまで検証できない** ため（特徴点が十分取れるか、収束するか、再投影誤差がどの程度かは設計書に書けない）。3か月目に「精度が出ない」と判明すると挽回不能。

一方、SE/SIer系を併願に残すなら設計書・テスト仕様書を書いた経験は評価される。また2人の分担がインターフェース定義に依存するため、設計フェーズ自体には強い意味がある。

→ **不確実性の高い前半を探索的に、不確実性が減った後半をウォーターフォール的に。**

### フェーズ計画

#### Phase 0：技術検証（3週間、ドキュメント最小）

既存ライブラリを繋いで一直線に通す。OpenCVで特徴点、`solvePnP`で姿勢、`triangulatePoints`で点群、Open3Dで表示。

**目的は「自分たちの撮影環境で特徴点が何点取れるか」「再投影誤差が何pxか」の実測値を得ることだけ。** 数字が出ないなら被写体か照明を変える。

※ この段階で **Blender内蔵Pythonのバージョンを確認** すること（§8参照）。

#### Phase 1：要件定義・基本設計（2週間）

Phase 0 の実測値を根拠に数値目標を置く。

例: カメラ角度誤差 2°以下 / 再投影誤差 1.5px以下 / 処理時間 10分以内 / Hausdorff距離で真値メッシュとの誤差 ○mm以下

ファイルインターフェース仕様を確定。

#### Phase 2：数理コアの自作（6週間）

DLT・三角測量・バンドル調整・CG法ソルバを自作し、Phase 0 のライブラリ版と結果を突き合わせる。
**ライブラリ版が正解のリファレンスになるため、デバッグが劇的に楽になる。** これがPhase 0を挟むもう一つの理由。

#### Phase 3：Visual Hull統合とメッシュ化（4週間）

ボクセル彫刻 → Marching Cubes → 平滑化 → テクスチャ → Blender出力

#### Phase 4：評価実験（3週間）

- 画像枚数 vs 復元精度（5/10/20/40枚）
- 撮影角度刻み vs 三角測量の条件数 vs 誤差
- バンドル調整の有無による精度差
- CG法の前処理あり/なしでの反復回数・計算時間
- **①のみ / ④のみ / 統合 の3条件比較** ← 統合の妥当性を数字で証明する最重要実験

---

## 8. 技術スタック（確定）

### 使用言語：Python 単一言語

**確定理由:**

1. **Blender が Python API しか受け付けない。** 出口が固定されている以上、全体を揃えるのが最も摩擦が少ない。
2. OpenCV / Open3D / scikit-image は全て Python ファースト。C++にすると松田の作業速度が落ちる。
3. PRで相互レビューする設計上、片方が読めない言語が混ざるとレビューが形骸化する。

### 自作とライブラリの線引き

| レイヤ | 扱い |
|---|---|
| BLASレベル（行列積、ノルム、疎行列matvec） | NumPy / SciPy に任せる |
| **反復解法（CG法、前処理、収束判定）** | **自作** |
| **最適化（Gauss-Newton、LM、Schur補元）** | **自作** |
| **幾何アルゴリズム（DLT、三角測量、ボクセル彫刻）** | **自作** |
| I/O、可視化、画像前処理、メッシュ変換 | ライブラリに任せる |

`A @ x` を自作する意味はゼロだが、その上に載るCG法・LM法は完全に自作範囲。学部でも研究でも疎行列演算をBLAS/SpMVに乗せるのは普通であり、欠点にならない。

### 性能面の懸念と対処

ボクセル彫刻が最大の負荷（256³ ≈ 1670万ボクセル × 20枚 = 3億回の投影）。

**forループではなく行列演算として書く。** 全ボクセル中心を `4×N` の同次座標行列にまとめ、カメラ行列を左から一括適用する。

```python
# ループで書く（× 数時間）
for v in voxels:
    for cam in cameras:
        p = cam.P @ v

# 行列で書く（○ 数秒）
uv = P @ V          # P: 3×4, V: 4×N
```

この書き換え自体が実験項目になる（ナイーブ実装との速度比較）。
逃げ道として Numba の `@njit`（デコレータ1行で10〜100倍）。

### C++ / Rust を採用しない理由

- C++: CMake、Eigen、OpenCVビルド、2台のマシンの環境差 → **環境構築で2週間溶けるリスク**。しかも結局Blender連携でPythonが必要。
- Rust: この領域のエコシステムが薄い。Rustは別のCLIプロジェクトで示すほうが確実。

**どうしても入れる場合の推奨:** Phase 4 でプロファイリングし、最も重い1関数だけを pybind11 でC++化する。「ボトルネックを計測で特定し、該当箇所のみC++で置換して○倍高速化」のほうが全面C++より説明として強く、リスクも局所化される。

### Pythonバージョン 【2026-08-15 確定】

**3.11 で確定。** Blender内蔵Pythonに合わせるため（ずれるとBlender内で自作モジュールをimportできない）。

~~ただしBlenderのバージョンで内蔵Pythonが異なるため、Phase 0 の最初に実機で確認して確定すること。~~
→ **松田の実機で確認完了：Blender 5.0.1 / 内蔵Python 3.11.13**（原本 `blender_system/system-info.txt`）。**執筆時に想定していた 4.2 LTS ではなく 5.0.1 だったが、内蔵Pythonは同じ 3.11 系だったため、推奨値がそのまま確定値になった。** 詳細は計画書 §7-2・§7-6。

### ライブラリ構成

| 用途 | ライブラリ | 担当 |
|---|---|---|
| 数値計算基盤 | NumPy, SciPy (sparse) | 関口 |
| 画像処理・特徴点 | OpenCV | 松田 |
| 点群・メッシュ | Open3D | 松田 |
| 3D出力 | Blender Python API | 松田 |
| 可視化・グラフ | Matplotlib | 両者 |
| テスト | pytest | 両者 |
| 高速化（必要時） | Numba | 関口 |

Phase 0 で使うOpenCVの `solvePnP` / `triangulatePoints` は、Phase 2 で自作版に差し替えたあとも **回帰テストのリファレンスとして残す。**

---

## 9. リスク管理：撤退ライン

野心的な構成のため、撤退ラインを先に決めておく。上から順に諦める。

| 段階 | 到達内容 | 判定 |
|---|---|---|
| L4 | 統合 + 実験 + GUI + ドキュメント一式 | 理想 |
| L3 | 統合パイプライン完成、Blender出力、比較実験 | 目標 |
| L2 | ④のみ（既知カメラ位置でのVisual Hull）でメッシュ生成 | 合格ライン |
| L1 | ①のみ、自作バンドル調整で点群生成 + 収束実験 | 最低保証 |

L1とL2は独立して成立するため、**3か月目時点の進捗で切り替え可能。**
- バンドル調整が収束しない → L2へ（ターンテーブル角度からカメラ位置を直接計算し、姿勢推定を回避）
- メッシュ化が破綻 → L1へ

この撤退ラインの存在が、ウォーターフォール的計画の最大の弱点（後半での破綻）を吸収する。

---

## 10. 未決定事項（次にここから再開）

推奨順序は **3 → 5 → 1**（インターフェースと座標系が決まれば並行着工できるため）。

1. リポジトリ構成とディレクトリ設計
2. 環境の再現方法（`uv` か `venv` + `requirements.txt` か。2台で同一環境をどう作るか）
3. **`cameras.json` のスキーマ**（①と④の接続点。最優先）
4. ブランチ戦略とIssueの粒度
5. **座標系の規約**（右手系/左手系。OpenCVとBlenderで流儀が違い、事故が起きやすい）

### その他の未決定・未確認事項

- 松田の志向（リアルタイム描画寄りか、静止画処理寄りか）の確認 → **未解決。確認シート Q2 で照会中。優先度は上がっている**（計画書 §7-6）
- ターンテーブルの調達方法、背景布、照明の具体 → **未解決**（確認シート Q14）
- ~~Blenderのバージョンと内蔵Pythonバージョン~~ → **✅ 解決（2026-08-15）：Blender 5.0.1 / Python 3.11.13**
- ~~ウォーターフォール検討の動機が「大学のPBL要件」か「就活アピール」かの確認~~ → **✅ 解決：大学の授業・PBLとは無関係。完全な自主プロジェクト**（計画書 §0）
- ~~GitHub Actions によるCI導入の要否~~ → **✅ 解決：導入する。Ubuntu + Windows の2環境で pytest**（計画書 §9-2）

---

## 11. 成果の語り口

同じ成果物を、相手によって別の切り口で提示できることが本テーマの利点。

### 就活面接

> 多視点復元は無地の面で点群が欠落し、シルエット法は凹形状を復元できないという相補的な弱点があります。そこで前者で推定したカメラ姿勢を後者の彫刻に流用し、さらに点群で凹部を補正する統合パイプラインを設計しました。単独手法との比較実験で、統合により表面誤差が○%改善することを確認しています。

### 開発手法について

> 3D復元は精度が実測しないと分からないため、最初の3週間を技術検証に充て、そこで得た再投影誤差の実測値をもとに要件を定義しました。以降は設計→実装→テストの工程を切って進め、インターフェース仕様を先に固めることで2人の作業を並行させました。

### 研究室訪問・院試

> バンドル調整の縮小系をCG法で解く部分を自作し、前処理による反復回数の変化と、視差角が小さいときの条件数悪化を測定しました。

---

## 12. 検討したが採用しなかった案（再提案不要）

| 案 | 不採用の理由 |
|---|---|
| 物理シミュレーション × Unity | 数学的な芯（圧力ポアソン方程式のCG法）は優秀だが、Unityと数値計算部分の言語をまたぐ接続コストが高い。第二候補として残置。 |
| 画像復元・悪条件逆問題（CGLS + Tikhonov） | 研究接続は最高、完遂リスクも最低。ただし松田のCG専門が活きる範囲が狭い。**共同開発である以上、松田が「自分の仕事」と感じられるかは無視できない**ため見送り。 |
| ラプラシアン系の画像・メッシュ処理 | ソルバとしての難易度が一段低く、研究接続もやや弱い。 |
| 画像処理の自動最適化 / 画像の美しさの数理評価 / 避難・交通シミュレーション | 数理的な芯が細く、評価が主観に寄る。 |
| ②を単独で実施（ライブラリ依存のSfM） | COLMAP/OpenCVを呼ぶだけで中身がブラックボックスのまま終わるリスク。→ **自作範囲を明示的に確保することで解決済み**（§8）。 |
