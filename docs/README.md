# 3D復元プロジェクト — ここから読む

**迷ったらこのファイルに戻ってくれば大丈夫です。**
他の文書は全部「必要になったときに引くもの」なので、頭から読む必要はありません。

**最終更新**: 2026-08-20（**ディレクトリ骨格が完成。`uv run pytest` が `1 passed` になり、G6 の「器」が全部そろいました**。あわせて **clone 先の注意**（§7-8）を追加）

---

## 1. いま、どこにいるか

```
着工  ●━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  完成
       ↑
   いまここ（2026-08-20・W01 4日目）
```

| | 状態 |
|---|---|
| 計画 | ✅ できている |
| Python のバージョン | ✅ 3.11 に確定（Blender 5.0.1 の内蔵版に合わせた） |
| 確認シートの回答 | ✅ **受領済み（8/16）。着工をブロックするものは残っていない** |
| `.gitattributes` | ✅ **完了（8/17）。計画書 §7-3 の6行すべて入った**（`eol=lf` とバイナリ5行を追記済み） |
| `pyproject.toml` / `uv.lock` | ✅ **作成・コミット済み（8/17）** |
| `.gitignore` | ✅ **完了（8/19）＋ `/data/` への修正も完了（8/20）**（`.venv/` `__pycache__/` `*.pyc` `/data/`）。`tests/data/` を巻き込まないことを `git check-ignore` で確認済み |
| pytest（`uv add --dev pytest`） | ✅ **完了（8/19）。** `uv.lock` に pytest 9.1.1 が入った |
| remote URL → `4bitcom_HandM` | ✅ 完了（`git remote -v` で確認済み。計画書 §13 ①） |
| `.python-version`（`3.11.13`） | ✅ **完了（8/20）。** `uv sync` で `.venv` を 3.11.13 で作り直し済み（Blender 5.0.1 の内蔵版と patch まで一致） |
| ディレクトリの骨格 | ✅ **完了（8/20）。** `src/recon3d/` の7パッケージ＋`tests/`＋`tools/` を作成。**`uv run pytest` が `1 passed`**（[計画書 §6-2](plan.md) の完了判定を満たした） |
| `import recon3d` | ✅ **通った（8/20）。** `[build-system]`（hatchling）で editable install（[計画書 §6-3](plan.md)・[D-38](decisions.md#d-38)） |
| **G6（基盤）の「器」** | ✅ **全部そろいました。** ここから先は**中身の実装**（W01 ペアプロ#1 の `io/coords.py`・`io/cameras.py` から） |

### 8/20 までに進んだこと

- `.gitignore` を追加（8/19）。`uv sync` で `.venv/` が生えても混ざらなくなった
- `uv add --dev pytest` 相当が入り、`uv.lock` が再生成された（pytest 9.1.1）
- `.gitignore` の `data/` を `/data/` に修正（8/20）。`tests/data/` が push できる状態になった
- **`.python-version`（`3.11.13`）を追加（8/20）。** `uv sync` が 3.11.13 を取得して `.venv` を作り直し、`uv run python -V` が 3.11.13 を返すことを確認済み。`uv.lock` は変化なし
- **骨格の範囲を [計画書 §6-2](plan.md) に明文化（8/20）。** 「`__init__.py` と `.gitkeep` だけ置いて `uv run pytest` が `1 passed` になるところまで」。**個別モジュール（`dlt.py` など）の空ファイルは作らない**
- **`import recon3d` の通し方を確定（8/20）。** `pyproject.toml` に `[build-system]`（hatchling）を足して **editable install** させる方式を採用（→ [計画書 §6-3](plan.md)）。pytest の `pythonpath` 案は不採用
- **ディレクトリ骨格を作成（8/20）。** `src/recon3d/{io,geometry,solvers,vision,mesh,fusion,viz}/__init__.py`・`tests/test_import.py`・`tests/{fixtures,data}/`・`tools/`。**`uv run pytest` → `1 passed`**、`uv run python -c "import recon3d"` も通ることを mac で確認済み
- **Discuss.md 8/19 の指摘は ①②③すべて解消しました**（③の `uv run pytest` が exit code 5 だった件は、smoke test が1件通ったことで解消）


> ⚠️ **他の端末は `git pull` のあと `uv sync` を1回流してください。** `pyproject.toml` に `[build-system]` が入ったので、プロジェクト自身が editable install されます（`uv.lock` も `virtual` → `editable` の1行だけ変わっています）。

### 8/16 に決まったこと

**① 確認シートの回答で片付いたこと**

| 分かったこと | 効果 |
|---|---|
| テーマとの相性・リアルタイム描画なしを**了承** | **テーマ再検討の分岐が閉じた。** このまま進めてよい |
| 開発機は **メモリ 16GB / i7-1360P 相当** | **ボクセルは 256³ のまま進められる** |
| Blender内蔵 **numpy 1.26.4** あり | Blenderスクリプトの書き方が変わらずに済んだ |
| **このリポジトリを3D復元専用にしてよい** | 完成後に public 化する前提が確定 |
| 「レンダリング技術の設計もやりたい」という希望 | 実験 **E8** の追加候補として受けた（[計画書 §11](plan.md)） |
| Python歴は数か月・**生成AIを使わずに書きたい** | 入口の段差を先に均す（[計画書 §7-7](plan.md)） |

**→ 回答は全て [計画書](plan.md)（§7-6 環境・§7-7 前提スキル・§14 確認事項）と [決定記録](decisions.md)（D-30〜D-37）に反映済みです。反映が済んだので、確認シートのファイル自体は 8/17 に削除しました。**

**② 分担のやり方を変えた**

**人ごとの担当表は廃止しました。** 代わりに**作業ジャンル G1〜G9**（[計画書 §3-3](plan.md)）を置いて、**誰がやるかは着手する場面で決めます。**

- 人が増えても表を書き直さなくていい
- 「今週これをやりたい」がそのまま通る
- **そのまま「こういう作業があります」という案内になる**（メンバーを誘うときに使える）

**あわせて、個人の研究志望に紐づく記述（技術核3つ・研究助走・志望分野）を計画書から外しました。** **やることは1つも減っていません**（→ [D-36](decisions.md#d-36)・[D-37](decisions.md#d-37)）。

---

## 2. 次にやること（これだけ）

**担当者は書きません。** 取れる人が取ってください（[計画書 §3-2](plan.md)）。詳細は [計画書 §13](plan.md) のチェックリスト。

### ✅ G6（基盤）の「器」は完了しました（8/20）

**1〜5 は全部終わっています。** ここから先に進んでよい状態です。**次にやることは下の「G7・G9」と「ペアプロ#1」です。**

1. ~~**`.gitattributes` を置いて push**~~ **✅ 完了（8/17）。** [計画書 §7-3](plan.md) の6行（`eol=lf` ＋ バイナリ5種）まで入ったので、**W02 の実データ投入前の宿題も消えた**
2. ~~`pyproject.toml`（Python 3.11 指定）、`uv sync`、`uv.lock` をコミット~~ **✅ 完了（8/17）**
3. ~~**`.gitignore` を置く**~~ **✅ 完了（8/19）＋ `/data/` への修正も完了（8/20）。** `tests/data/`（CI用にコミットする分。[計画書 §6](plan.md)）を巻き込まないことを確認済み
4. ~~**`pyproject.toml` に pytest を足す**~~ **✅ 完了（8/19。pytest 9.1.1）**
5. ~~**ディレクトリの骨格**（範囲は [計画書 §6-2](plan.md)）~~ **✅ 完了（8/20）。** `[build-system]` の追加 → `src/recon3d/` の7パッケージ → `tests/test_import.py` の3手順が終わり、**`uv run pytest` が `1 passed`**。**中身の実装（`dlt.py` など）はここには含みません**。各ジャンルの担当週に、実装する人が作ります
5-b. ~~**`.python-version` に `3.11.13` を書く**~~ **✅ 完了（8/20）。** ⚠️ **他の端末は `git pull` のあと `uv sync` を1回流してください**（`.venv` が 3.11.13 で作り直されます）

### 次に：G7・G9（CIと運用の器）

6. CI の雛形、`main` ブランチの保護、GitHub Projects のボード（**権限は受領済み**）
7. **マスクの作り方の候補を1枚にまとめる**（→ W02 に決める。**やったことがある人がいないので、まず材料を作るところから**）
8. **`docs/notes/` に Blender スクリプトの入口メモを1本**

### G2（3DCG）

9. **合成データに使う3Dモデルを探す** — **凹みのある形（マグカップのような内側のくぼみ）が必須**です。配布モデルでかまいません。**ライセンス表記だけ見ておいてください**

### 全員

10. clone して、**自分の環境で `uv sync` と `uv run pytest` が通るか確認**（**いま回せます。`1 passed` になれば成功**。Windows / Ubuntu の2台がまだ未確認です ← **ここが次の宿題**）
11. **Git の練習1時間**。branch → PR → レビュー → merge を1周します

### 全員でやること

**W01 にペアプロ 2時間。** 座標系の決まりごとと `cameras.json` の形式を、参加できるメンバー全員で決めて実装します。**ここが決まらないと誰も先に進めない**ので、最初にやります。

**あわせて、8/16 の確認で答えを保留した分を口頭で決めます**（15分くらい。→ [計画書 §14](plan.md) の 10〜12）。

- 最終的に何を成果物として出したいか（動画・glTF・.blend など）
- 撤退ライン（11/1・11/29 の判定）の認識合わせ ← **ここは事前に合わせておかないと、判定の場で揉めます**
- 週の進め方・連絡手段
- メッシュ処理をどこまで自作するか

---

## 3. どの文書を、いつ開くか

**普段は開かなくていいです。** 下の「こういうとき」に当てはまったときだけ開いてください。

| こういうとき | 開くもの |
|---|---|
| **何をすればいいか分からない** | **このファイル（README）** |
| **次に何を作業するか選ぶ** | [計画書 §3-3](plan.md) の**作業ジャンル一覧（G1〜G9）** |
| **新しく入った人に渡す** | [計画書 §3-3（作業一覧）と §3-8（スコープ）](plan.md) |
| 「あの確認、何て答えが出てたっけ？」 | [計画書 §14](plan.md)（8/16 の回答と、その反映先の一覧） |
| 「この仕様どうなってた？」 | [計画書](plan.md) |
| **「なんでこう決めたんだっけ？」** | [決定記録](decisions.md) |
| 「この決定、変えたい」 | [決定記録](decisions.md) で理由を読んでから相談 |
| 今週の作業が終わった | [進捗ログ](progress.md) に記録 → **書けたらAIに見せる**（抜けを指摘してもらう） |
| 新しくAIに相談する | `internal/ai-session-context.md` を渡す |

### ファイルの一覧

**読むもの（4つ）**

| ファイル | 何が書いてあるか | いつ |
|---|---|---|
| **README.md**（これ） | 現在地と次の一手 | **迷ったら毎回** |
| [計画書](plan.md) | 決まったこと・仕様・18週の日程（**8/16 の確認シートの回答もここに入っている**） | 引くとき |
| [決定記録](decisions.md) | **なぜそう決めたか**（D-01〜D-37） | 引くとき |
| [進捗ログ](progress.md) | 実際どう進んだか（**テンプレをコピーして5分**） | 毎週 |

**リポジトリのルート直下（`docs/` の外）**

| ファイル | 何が書いてあるか | いつ |
|---|---|---|
| [Discuss.md](../Discuss.md) | **非同期の連絡場所。** 対面でないときの連絡と、完了報告への指摘 | **定期的に確認** |
| [HowToPush.md](../HowToPush.md) | Git の操作手順（旧 ルート `README.md`） | Git で詰まったとき |

**読まなくていいもの（`internal/`）**

手続き用・履歴用です。中身が要るときだけ開けば十分です。

| ファイル | 中身 |
|---|---|
| `internal/ai-session-context.md` | AIに文脈を渡すための資料 |
| `internal/progress-log-review.md` | **進捗ログをAIにレビューさせる手順**（判定基準・週ごとの観測ポイント） |
| `internal/project-handoff.md` | テーマを選んだときの検討経緯（8/14時点） |
| `internal/partner-questions-v1.md` | 確認シート第1版。回答済みの記録と**用語集** |

---

## 4. 各ステップで迷子にならないための地図

**先の話なので、いまは読まなくて大丈夫です。** そのフェーズに入ったときに戻ってきてください。

| 時期 | やること | 開く文書 |
|---|---|---|
| ~~**いま〜8/16**~~ | ~~確認シートの回答~~ **✅ 完了（8/16。回答は計画書・決定記録に反映済み）** | — |
| **W01**<br>8/17〜 | 環境構築、ペアプロ#1（座標系と `cameras.json`）、Git練習1h、保留分の口頭確認 | 計画書 §4・§5・§13・§14 |
| **W02–W03** | G2：Blenderで合成データ作成<br>G1・G3：OpenCVで一直線に通してみる | 計画書 §10 Phase 0 |
| **W04–W05** | 実測値を根拠に「何ができたら合格か」を決める | 計画書 §10 Phase 1 |
| **W06–W11** | 数理コアの自作（一番重い6週間） | 計画書 §10 Phase 2 |
| **★11/1** | **1回目の判定。**うまくいってなければ規模を縮小 | 計画書 §12 |
| **W12–W15** | ボクセル彫刻・メッシュ化。ペアプロ#2・#3 | 計画書 §10 Phase 3 |
| **★11/29** | **2回目の判定** | 計画書 §12 |
| **W16–W18** | 評価実験、README整備 | 計画書 §11 |

**★の判定を先に決めてあるのは、諦めどきで揉めないためです。** どちらに転んでも成果物にはなるように組んであります。

---

## 5. 困ったときのメモ

**「これ何だっけ」という用語** → `internal/partner-questions-v1.md` の末尾に用語集があります（点群・メッシュ・ボクセル・バンドル調整など）。

**「勝手に決めていいのか分からない」** → 決めていいです。ただし**決めたことは必ずIssueかPRのコメントに1〜2行残してください。** 毎日会えるぶん口頭で済みがちですが、それだとGitHubに何も残らず、就活で見せる材料が減ります。

**「これ誰がやるんだっけ」** → **決まっていないのが正常です。** [計画書 §3-3](plan.md) に**作業ジャンル（G1〜G9）**の一覧があるので、**着手するときに取って、Issueに自分を入れてください。** 人ごとの担当表は 8/16 に廃止しました（→ [D-36](decisions.md#d-36)）。

---

## 6. まだ決まっていないこと

| | いつ決める |
|---|---|
| ~~開発機のメモリ → ボクセルの細かさ~~ | **✅ 16GB 前提。256³ のまま進める**（最終確認は W12 の実測） |
| 合成データに使う3Dモデル（**凹みのある形が必須**） | W01。**配布モデルを探す方向**（ライセンス確認が要る） |
| マスクの作り方 | W02。**やったことがある人がいないので、まず候補を1枚にまとめてから決める** |
| 保留分（成果物の形・撤退ライン・週の進め方・メッシュ自作範囲） | **W01 の対面で口頭** |
| レンダリング設計をどこまでやるか（実験 E8 の採否） | W04（W02 の実装の余裕を見てから） |
| 数値目標の具体値 | W04（実測してから） |
| 撮影機材（**三脚・回転台・背景布・照明が無い**） | W03まで。**予算の相談が要る** |
| ~~`doc/` を `docs/` に改名＋**ファイル名を短くする**~~ | **✅ 8/17 完了。** `plan.md` / `decisions.md` / `progress.md` に改名しました（「似た名前で長い」との指摘への対応） |
| **メンバーを増やすか（増やすなら誰を・何ジャンルを渡すか）** | 随時。**3人なら1人あたり週5.8h**（計画書 §3-1） |
