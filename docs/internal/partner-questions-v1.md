# 3D復元プロジェクト — 着工前の確認シート（第1版・記入済み）

> **🗄 このシートには書き込まないでください。役目は終わっています。**
> **未回答分は第2版（2026-08-16 に回答受領）で解消済み**で、回答の中身は [計画書](../plan.md) と [決定記録](../decisions.md) に反映してあります（第2版のファイルは反映完了後の 2026-08-17 に削除しました）。**以降の確認は文書を作らず口頭で行い、決まったことを Issue / PR に1〜2行残します**（計画書 §8-3）。
> **用語集は [docs/glossary.md](../glossary.md) に移しました（2026-08-24）。** 分からない言葉はそちらを引いてください。本書末尾のものは移行前の原本です。

**宛先**: 松田
**作成**: 関口 / 2026-08-14
**最終更新**: 2026-08-15（松田から `blender_system/system-info.txt` を受領し、Q9 / Q10 / Q13 / Q16 に反映）
**所要時間**: 20〜30分

---

## 回答受領状況（2026-08-15 時点）

松田から **Blender のシステム情報**（`blender_system/system-info.txt`、コミット `aa97607`）を受領した。これにより以下が埋まった。

| 設問 | 状態 | 出典 |
|---|---|---|
| **Q9** Blender / Python のバージョン | **✅ 回答完了** | `blender_system/system-info.txt` |
| **Q10** Windows のバージョン・開発環境 | 🔶 部分回答（OS のみ判明。エディタ・WSL・ターミナルは未回答） | 同上 |
| **Q13** PCスペック | 🔶 部分回答（GPU・CPU命令セットのみ判明。**メモリ・ストレージが未回答**） | 同上 |
| **Q16** 3DCGの経験 | 🔶 間接的な手がかりのみ（有効アドオンから推測。**本人の回答が必要**） | 同上 |
| Q1–Q8, Q11, Q12, Q14, Q15, Q17–Q24 | ⬜ 未回答 | — |

**まだ回答が必要なもののうち、優先度が高い3つ**

1. **Q1・Q2・Q3**（Noなら計画そのものを組み直す設問）
2. **Q13 のメモリ容量** — ボクセル解像度 256³ を確定できない
3. **Q15 志望研究室の分野** — 受領した情報からは**キャラクター/アニメーション寄り**の可能性が読み取れるため（Q16 の項を参照）、確認の重要度が上がった

> ## 📄 上記の未回答分は第2版で解消済み（2026-08-16）
>
> 上記の未回答分と、受領情報から新たに生じた質問（N1–N10）をまとめた**確認シート第2版**に、松田から 2026-08-16 に回答を受領した。**回答の中身は計画書 §7-6（環境）・§7-7（前提スキル）・§14（確認事項）と決定記録 D-30〜D-37 に反映済み**で、第2版のファイルは反映完了後の 2026-08-17 に削除した。
>
> **答えを保留した分（成果物の形・撤退ライン・週の進め方・メッシュ自作範囲）は W01 の対面で口頭確認する**（計画書 §14 の 10〜13）。
>
> **本書（第1版）は、受領済み回答の記録と用語集として残す。**

**資料セット**（この3つで文脈が完結する）

| # | ファイル | 内容 |
|---|---|---|
| 1 | `project-handoff.md` | テーマ選定の経緯・数理的な芯（背景資料） |
| 2 | `../plan.md` | **開発計画書・全16節。現在の正典** |
| 3 | **本書** | 松田への確認シート（Q1–Q24） |

**回答方法**: このファイルをコピーして各設問の `回答:` 欄に直接書き込み、返してもらう形が一番早い。全部埋まらなくてもよい。**Part 1 の Q1・Q2・Q3 だけは必ず答えてほしい**（Noなら計画を組み直すため）。

---

## このシートについて

計画書を一通り書いたので、**着工前に君の合意と情報が必要な項目**をまとめた。計画書を全部読まなくても答えられるように、必要な前提は各設問に書いてある。

3部構成。

| Part | 内容 | 答え方 |
|---|---|---|
| **Part 1** | 合意してほしいこと（8問） | Yes / No / 条件付き |
| **Part 2** | 君しか知らない事実（8問） | 事実を記入 |
| **Part 3** | 一緒に決めたいこと（9問） | 意見・希望を記入 |

**「No」や「気が進まない」も歓迎する。** 着工後に判明するより、今この段階で分かったほうが圧倒的に安い。特に Part 1 の Q1–Q3 は、Noなら計画を組み直す。

各設問に **【これが変わる】** として、答えによって何が動くかを書いてある。

---

# Part 0. プロジェクトの30秒要約

**作るもの**: 物を一周撮った写真から3Dモデルを作るシステム。

**2つの手法を統合する。** それぞれの弱点が正反対だから。

| 手法 | 得意 | 不得意 |
|---|---|---|
| ① 多視点復元（特徴点→カメラ姿勢→点群） | 模様のある面、凹み | 無地の面（点群に穴が空く） |
| ④ シルエット復元（輪郭→ボクセル彫刻→メッシュ） | 外形が確実、必ず閉じた形になる | **凹みが原理的に削れない**（お椀の内側が埋まる） |

**合流点は2つ。**
1. ④が「削る」にはカメラの角度が要る → ①が供給する
2. ④は凹みを削れない → ①の点群が「ここは物体の外だ」と教えて補正する

**期間**: 2026-08-17 着工 〜 12-20（18週）。1人あたり週8〜9時間。
**言語**: Python単一。Blender が Python API しか受け付けないため。
**役割**: 関口＝数値計算（カメラ姿勢推定・最適化・反復解法）、松田＝CG・画像処理・メッシュ・データ生成。

---

# Part 1. 合意してほしいこと

## Q1. 君の「技術核」3つ、これで納得できるか？ 【最重要】

正直に書く。**最初の計画では、君の担当が「作業の受け皿」になっていて、3DCG研究への助走になっていなかった。** 関口側は「悪条件最小二乗＝志望研究室のテーマ」に直結する部分を4つ確保していたのに対して、君側にそれに相当するものが無かった。片方だけが得をする構造で、これは共同開発として良くない。

そこで、君側にも研究につながる技術核を3つ確保した。

**核1：合成データセット生成器（Blender）**

Blenderに既知の3Dモデルを置き、カメラを円周上に20方向並べてレンダリング。画像・シルエット・カメラ行列・正解の3D形状を一括出力する。**着工1週目から君が主役になる部分。**

**核2：メッシュ処理（Marching Cubes 以降）**

ボクセル（3Dのピクセル）の塊からメッシュを起こし、簡略化・UV展開・テクスチャ投影まで。

**核3：離散ラプラシアン作用素の構成（関口と共同）**

メッシュを滑らかにする処理で `(I + λL) x' = x` という方程式を解く。この `L` という行列の作り方を君が担当する。

| 担当 | 内容 | 分野 |
|---|---|---|
| **君** | 行列 `L` の構成。**一様重み vs cotan重み**（三角形の角度から決まる重み）を両方実装し、形の歪み方の違いを測る | 離散微分幾何 |
| 関口 | できた疎行列を共役勾配法で解く。反復回数・収束を測る | 数値線形代数 |

**核3が一番大事だと思っている。** ここが2人の専門が実際に交わる唯一の点で、「分担して2つ作った」ではなく「**同じ数式を、幾何側と解析側から2人で作った**」と言える。就活でも研究室訪問でも、この1点があるかないかで話の強さが変わる。

**【これが変わる】** Noなら役割分担を組み直す。何が足りないかを具体的に教えてほしい。

```
回答:
```

---

## Q2. このプロジェクトにリアルタイム描画は登場しない。了承できるか？ 【最重要】

君の興味は「リアルタイム・Unity・GUI寄り」と聞いた。**正直に言うと、このプロジェクトは静止画の一括処理で、リアルタイム描画は本質的に出てこない。** Unityも、数値計算部分との言語をまたぐ接続コストが高いため不採用にしてある。

だから君の旨味は、**リアルタイム描画ではなくジオメトリ処理とレンダリングに置くことになる。** 3DCG研究の評価軸としてはジオメトリ処理のほうがむしろ王道ではあるが、君がやりたいことと違うなら意味がない。

**受け皿として用意してあるもの**: ボクセルが削られていく過程をリアルタイム表示するビューア。デモ動画として一番見栄えがする部分でもある。ただし優先度は最後（余力があれば）。

**【これが変わる】** 「どうしてもリアルタイム描画を主軸にしたい」なら、このテーマ自体を再検討したほうがいい。第二候補として「物理シミュレーション × Unity」という案が残してある。着工前なら乗り換えのコストはゼロだが、着工後だと全部が無駄になる。**今言ってほしい。**

```
回答:
□ 了承する。ジオメトリ処理で問題ない
□ 了承するが、ビューアは優先度を上げたい
□ リアルタイム描画を主軸にしたい（テーマ再検討）

コメント:
```

---

## Q3. 「主担当＋副担当」という進め方でいいか？

「二人で協力して作りたい」という話があったので、その形を設計した。ただし**全部を2人で書くと開発速度が半分になり、確実に間に合わない**（工数計算済み・後述）。そこで折衷案。

| 役割 | やること |
|---|---|
| **主担当** | 実装する。設計判断をする |
| **副担当** | PRをレビューする。**「何をやっているか説明できる」ところまで理解する。** ~~実装はしない~~ |

つまり**全てのコードに、書く人と読む人がいる。** 書く1人＋読む1人なら消費は1.3倍程度で済むが、書く2人だと2倍になる。工数からしてこれが上限。

> ## ⚠ この設問は 2026-08-15 に修正された
>
> **「副担当は実装しない」は書き方の誤りだった。** 実際に想定しているのは「**詳しいほうが主軸になって、一緒に作る**」形であり、**副担当も実装に参加する。**
>
> **修正版（第2版 Q-e）に松田は「了承」と回答済み（2026-08-16）。** ただし**この「主担当＋副担当」という枠組み自体が同日にジャンル制へ改訂された**（→ [D-36](../decisions.md#d-36)）。上の表は経緯の記録として残してある。
>
> 修正後の定義（計画書 §3-2・§3-6 が正典）:
>
> | 役割 | やること |
> |---|---|
> | **主担当** | **設計を決める。** モジュールの中核を実装する。副担当がやる部分を切り出す |
> | **副担当** | **切り出された部分を実装する。** そのうえでPR全体をレビューして理解する |
>
> 切り出しの原則は「**副担当自身の専門が顔を出す部分を選ぶ**」。具体的な割り当ては計画書 §3-6 の担当表を参照。

**あわせて1つルールを入れたい。**

> PRを承認するとき、コメントに「**このPRが何をしているか、自分の言葉で1〜3行**」を必ず書く。

「LGTM」だけの形式的な承認を構造的に禁止するため。書けないなら、書けるまで聞く。よく会えるので、その場で聞けばいい。

**これは就活でも効く。**「相互レビューで互いの専門を理解した」を、GitHubのPR履歴という証拠付きで語れる。

**【これが変わる】** Noなら、共有をもっと増やす代わりに作るものを減らす（＝スコープ削減）というトレードオフになる。

```
回答:
```

---

## Q4. 対面ペアプロを3か所・計12時間、確保できるか？

速度を犠牲にしてでも2人で書く価値がある箇所を、**3つだけ**選んだ。

| 箇所 | 2人でやる理由 | 時間 | 時期 |
|---|---|---|---|
| `cameras.json` の入出力と座標系変換 | ここを間違えると全工程が汚染される。3D復元で一番事故が多い箇所 | 2h×1 | W01（8/17の週） |
| ボクセル彫刻の行列化 | カメラの幾何（関口）とボクセル/画像の座標（君）が噛み合う箇所 | 2h×2 | W12（11/2の週） |
| 離散ラプラシアン `L` の構成と求解 | Q1の核3。2人の専門が交わる点 | 2h×3 | W14（11/16の週） |

計12時間。総予算の約4%。**これ以上は増やせない。**

**【これが変わる】** 時期の都合が悪ければずらす。ただしW01のペアプロだけは動かせない（ここが決まらないと2人とも着工できない）。

```
回答:
```

---

## Q5. 「合成データから始める」方針でいいか？

**最初は実物を撮影せず、Blenderの中でレンダリングした画像から始めたい。**

理由は3つ。

**① 正解が全部手に入る。** 実物を撮影しても、そのマグカップの「正しい3D形状」は誰も知らない。だから復元結果が正しいかを数値で言えない。Blenderなら、元の3Dモデルがそのまま正解になる。カメラの位置も、シルエットも、全部正解が分かる。

**② デバッグが劇的に楽になる。** 誤差ゼロの完璧な入力を入れて答えが合わなければ、原因は100%プログラムのバグ。実物だと「バグなのか撮影が悪いのか」が切り分けられず、ここで数週間溶ける。

**③ 今日から始められる。** 回転台も背景布も照明も要らない。そして**君の最初の仕事が1週目に発生する**（関口の実装完了を待たなくていい）。

実物撮影は Phase 0 の最終週（W03、8/31の週）に**1セットだけ**試して感触を掴み、本格導入は Phase 3（11月）から。

**【これが変わる】** 「最初から実物でやりたい」なら、機材調達を最優先タスクにして日程を組み直す。

```
回答:
```

---

## Q6. 週8〜9時間 × 18週、コミットできるか？

全部作った場合の工数を積み上げると **315時間**。2人で割って18週で割ると、**1人あたり週8.75時間**（1日1〜2時間 × 週4日程度）。

| 項目 | 概算 |
|---|---|
| バンドル調整（最適化の中核） | 60h |
| 評価実験 | 40h |
| I/O・テスト・CI | 30h |
| DLT・三角測量 | 30h |
| ボクセル彫刻 | 25h |
| セグメンテーション・特徴点 | 25h |
| テクスチャ・UV展開 | 25h |
| Marching Cubes | 20h |
| ラプラシアン平滑化 | 20h |
| 合成データ生成器 | 20h |
| ドキュメント | 20h |
| **合計** | **315h** |

**注意点を正直に書く。** この見積もりに**デバッグ時間が入っていない。** 数値計算のデバッグは実装の1〜2倍かかるのが普通なので、実質450〜500時間になる可能性がある。その場合は5〜6か月（〜2027年2月）に延ばすか、後述の撤退ライン（Q22）で規模を落とす。

**大事なのはペース。**「休日に10時間、その後2週間放置」より「毎週コンスタントに」を優先したい。GitHubのコミット履歴が継続性の証拠になるため。

**【これが変わる】** 週の稼働時間が違うなら、スコープか期間を今のうちに調整する。

```
回答:
週に確保できそうな時間:
繁忙で動けない時期（バイト・授業・他プロジェクト）:
```

---

## Q7. Git運用のルール、これでいいか？

Git経験が浅いと聞いたので、できるだけシンプルにした。

| 項目 | 決定 |
|---|---|
| ブランチ | `main` 一本。作業は `feat/mesh-marching-cubes` のような枝を切る |
| `main` への直push | **禁止**（設定で弾く）。必ずPR経由 |
| マージ | squash merge（履歴が読みやすい） |
| レビュー | 相手の承認が必須。**ただし最初の2週間（W01–W02）は承認不要**にする。慣れてからW03で必須化 |
| Issue | 1 Issue = 1 PR = 1〜2週で終わる単位 |
| ボード | GitHub Projects を1枚（Todo / In Progress / In Review / Done） |

**週1回、対面15分で次週のIssueを2人で切る。**

**1つお願い。** 対面で決めたことは、その場でIssueかPRにコメントで残してほしい。**GitHub上に議論が残らないと、就活で見せられる材料が減る。** 口頭で全部済ませると、リポジトリには結果のコードしか残らない。

**【これが変わる】** Gitに不安があれば、W01で一緒に練習する時間を取る（1時間程度）。遠慮なく言ってほしい。

```
回答:
□ 問題ない
□ W01でGitの練習時間がほしい

不安な操作があれば具体的に:
```

---

## Q8. リポジトリを public にするのは完成後でいいか？

現在 private。**完成後（2026年12月以降）に public 化**する想定。

トレードオフを書いておく。

| | private → 後で public | 最初から public |
|---|---|---|
| 利点 | 試行錯誤の汚い履歴を整理してから見せられる | GitHubの草（contribution graph）が最初から公開で積み上がる |
| 欠点 | 「継続的に開発した」証拠が後出しになる | 最初からREADMEとコミットメッセージを見られる前提で書く必要がある |

関口としては現状の「完成後public」で問題ないと思っているが、就活の時期によっては早めに公開したほうがいい可能性もある。

**【これが変わる】** 早期公開にするなら、README とコミットメッセージの書き方の基準を最初から上げる。

```
回答:
```

---

# Part 2. 君しか知らない事実（教えてほしい）

## Q9. Blenderのバージョンと、内蔵Pythonのバージョン 【W01最優先】

**これが分からないとプロジェクトのPythonバージョンを確定できない。** ずれるとBlenderの中で自作モジュールを import できない。

**調べ方**: Blenderを起動 → 上部タブの「Scripting」→ 下部のコンソールに以下を貼って Enter。

```python
import sys, bpy
print("Blender:", bpy.app.version_string)
print("Python :", sys.version)
```

**【これが変わる】** プロジェクト全体のPythonバージョン。現状 3.11 で仮決めしている（Blender 4.2 LTS が 3.11 のため）。

```
回答: ✅ 2026-08-15 受領（blender_system/system-info.txt）
Blender: 5.0.1  (branch: blender-v5.0-release / hash: a3db93c5b259 / 2025-12-15 Release)
Python : 3.11.13  (MSC v.1929 64bit AMD64)
```

### 関口側の処理結果

**仮決めしていた Python 3.11 で正しかった。バージョン確定。**

| 項目 | 確定値 |
|---|---|
| Blender | **5.0.1**（Windows 版・公式インストーラ） |
| 内蔵 Python | **3.11.13** |
| プロジェクトの Python | **3.11 系に固定**。`uv` で `3.11.13` を明示指定して 3台の環境を揃える |

**当初の想定との差分**: 計画書は「Blender **4.2 LTS** が 3.11 だから」を根拠に 3.11 を仮決めしていたが、実機は **5.0.1** だった。**結果として Python は同じ 3.11 系なので、計画への影響はない**（根拠の記述だけ計画書 §7-2 で差し替えた）。

**ただし1点だけ注意が要る。** 5.0.x は LTS ではないため、松田側が Blender を自動更新すると内蔵 Python が動く可能性がある。

> **お願い**: 開発期間中（〜2026-12）は **Blender を 5.0.x から上げないでほしい。** 上げる必要が出たら、上げる前に一報がほしい（プロジェクトの Python を追随させる必要があるため）。

**受領した情報から追加で分かったこと**（Q10・Q13・Q16 に反映済み）は各設問を参照。

---

## Q10. Windows のバージョンと、開発環境

**【これが変わる】** 環境構築手順の書き方。WSLを使うなら話が変わる（Blenderとの連携が面倒になるので、素のWindowsを推奨したい）。

```
回答: 🔶 2026-08-15 一部判明（blender_system/system-info.txt より）
Windows のバージョン（10 / 11）: ビルド 10.0.26200 → 実体は Windows 11（25H2 相当）と思われる ※要確認
エディタ（VSCode / PyCharm / その他）: ← 未回答
WSL は使っている？: Blender は素の Windows 側に導入済み（C:\Program Files\...）。開発全体で WSL を使うかは ← 未回答
ターミナルは何を使っている？（PowerShell / Git Bash / cmd）: ← 未回答
```

### 補足（読み取りの根拠）

システム情報の `platform:` 行は `'Windows-10-10.0.26200-SP0'` となっているが、**これを「Windows 10」と読んではいけない。** Python の `platform` モジュールは Windows 11 でも先頭を `Windows-10` と表記するため、判定に使うのは末尾のビルド番号のほう。**26200 は Windows 11 系のビルド**（26100 = 24H2、26200 = 25H2）。

**確定したこと**: Blender は `C:\Program Files\Blender Foundation\Blender 5.0\` に入っており、**WSL 内ではなく素の Windows 上で動いている**。これは §7 で推奨していた形と一致するので、環境構築手順はそのまま使える。

**まだ聞きたいこと**: エディタ / ターミナル / WSL の使用有無。CI が Windows でも回る構成（§9）なので致命的ではないが、環境構築手順書（W01）を松田の実環境に合わせて書きたい。

---

## Q11. Git / GitHub の経験レベル

正確に把握したい。**できないことがあるのは全く問題ではない。** 把握できていないまま進めるのが問題。

```
回答（できるものにチェック）:
□ clone
□ commit / push
□ branch を切る
□ Pull Request を出す
□ Pull Request をレビューする
□ merge conflict を解決する
□ rebase
□ .gitignore を書く

GitHub でチーム開発をした経験:
```

---

## Q12. Python の経験レベル

```
回答:
Python 歴:
□ numpy を使ったことがある
□ OpenCV を使ったことがある
□ Open3D を使ったことがある
□ Blender の Python API（bpy）でスクリプトを書いたことがある
□ pytest を書いたことがある
□ クラス / dataclass を書ける
□ 型ヒントを書いたことがある

一番慣れている言語:
```

---

## Q13. 開発に使うPCのスペック

**【これが変わる】** ボクセルの解像度（256³ = 1670万個 を想定中）と、レンダリングの枚数・解像度。メモリが足りないなら128³に落とす。

```
回答: 🔶 2026-08-15 一部判明（blender_system/system-info.txt より）
CPU: 型番不明。AVX2 対応（Cycles が AVX2 カーネルを選択している）
メモリ: ← 未回答【最重要】
GPU（あれば型番）: Intel(R) Iris(R) Xe Graphics（内蔵GPU）
                   ドライバ 32.0.101.7085 / OpenGL 4.6 / Blender のバックエンドは OpenGL
ストレージの空き: ← 未回答
```

### ここから判明した重要な制約：**Cycles は CPU レンダリングになる**

システム情報の Cycles の欄に出ているのは `CPU device capabilities: AVX2` の1行だけで、**GPU 計算デバイス（OneAPI 等）が挙がっていない。** つまり現状の設定では **Cycles を選ぶとレンダリングが CPU で回る。**

内蔵GPU（Iris Xe）である点と合わせると、含意は3つ。

| 影響 | 対応（計画書 §7-6 に反映済み） |
|---|---|
| Cycles で20方向×複数データセットを回すと、レンダリングだけで数時間〜溶ける | **合成データ生成は EEVEE を既定にする。** 復元の入力に必要なのは「一貫した見え方」であって物理的に正確な光ではないため、EEVEE で十分 |
| 内蔵GPU はシステムメモリを共有する | ボクセル 256³ の可否がメモリ次第。**下の試算を参照** |
| ビューポート表示も内蔵GPU | L4 のリアルタイムビューア（Q2）の実現性は控えめに見積もる |

### ボクセル解像度の試算（メモリ回答待ちの理由）

256³ = **1677万ボクセル**。占有グリッド自体は小さい（uint8 なら 17 MB）が、**問題は §7-5 の「行列で一括投影する」実装のほうで、中間バッファが大きくなる。**

| 配列 | 256³ | 128³ |
|---|---|---|
| 占有グリッド（uint8） | 17 MB | 2 MB |
| 全ボクセル中心の同次座標 `4×N`（float64） | **537 MB** | 67 MB |
| 投影結果 `3×N`（float64・カメラ1台分） | **403 MB** | 50 MB |

素直に書くと 256³ で**一時的に 1 GB 前後**を使う。**メモリ 8 GB だと厳しく、16 GB なら（チャンク分割すれば）通る**、という線。

**対応方針**: どちらに転んでも動くよう、**ボクセルをチャンク（塊）に分割して投影する実装を最初から採る。** これは関口側（`geometry/carving.py`、ペアプロ#2）の設計に落とす。そのうえで解像度の最終決定は W12 に実測で行う。

**なのでメモリ容量だけは早めに教えてほしい**（`Win + Pause` キー、またはタスクマネージャー → パフォーマンス → メモリ）。

---

## Q14. 撮影機材の所持状況

実物撮影は Phase 3（11月）からだが、調達に時間がかかるものは早めに動きたい。

```
回答（持っているものにチェック）:
□ カメラ（スマホでも可）— 機種:
□ 三脚
□ 回転台（ターンテーブル）
□ 単色の背景布（緑・黒など）
□ 照明（デスクライトでも可）
□ チェッカーボード（カメラ較正用の白黒格子。印刷でも可）

調達できそうなもの / 予算感:
```

---

## Q15. 志望している3DCG研究室は、どの分野？ 【重要】

一口に3DCGと言っても方向がかなり違うので、**どこに力を入れるかの配分を変えたい。**

| 分野 | このプロジェクトで強化できる部分 |
|---|---|
| **ジオメトリ処理・形状解析** | 核2・核3を厚くする。cotan重み、メッシュ簡略化、形状の誤差評価を深掘り |
| **レンダリング・シェーディング** | 核1を厚くする。物理ベースレンダリング、多様な照明条件でのデータセット生成 |
| **アニメーション・物理** | 正直、このプロジェクトでは直接つながらない。第二候補テーマのほうが合う |
| **画像生成・ニューラルレンダリング** | 復元結果を NeRF / 3D Gaussian Splatting と比較する実験を追加できる |

**【これが変わる】** 技術核の配分と、評価実験に追加する項目。

```
回答:
志望研究室 / 分野:
そこで扱われているテーマ（分かる範囲で）:
```

---

## Q16. 3DCGで、すでに得意なこと・やったことがあること

**【これが変わる】** 既にできることは工数見積もりから引ける。逆に未経験の部分は学習時間を積む。

```
回答: ⬜ 本人の回答が必要（以下は受領ファイルからの推測にすぎない）
Blender歴:
モデリング経験:
テクスチャ・UV展開の経験:
レンダリング設定（ライティング、カメラ設定）の経験:
Unity歴 / 作ったもの:
その他（シェーダ、アニメーション等）:
```

### 受領ファイルからの推測（**要・本人確認**）

`system-info.txt` の「有効なアドオン」欄に、標準では入っていないものが3つ入っていた。

| アドオン | バージョン | 用途 |
|---|---|---|
| `vrm` | 3.21.0 | VRM（VTuber向け3Dアバター形式）の入出力 |
| `mmd_tools` | 4.5.8 | MMD（MikuMikuDance）モデル・モーションの取り込み |
| `rokoko-studio-live-blender` | 1.4.3 | Rokoko のモーションキャプチャを Blender にライブ接続する |

**ここから読めること**: 松田の Blender 経験は **キャラクター／アバター／モーション寄り**である可能性が高い。特に Rokoko のライブ連携は、静止画のレンダリングではなくリアルタイムのモーション流し込みのためのもの。

**これが効いてくる設問が2つある。**

- **Q2（リアルタイム描画は登場しない）**: 「リアルタイム・Unity寄り」という事前の把握と、この構成は整合する。**Q2 の回答の重要度がさらに上がった。**
- **Q15（志望研究室の分野）**: Q15 の表で「**アニメーション・物理** → 正直、このプロジェクトでは直接つながらない」と書いた欄に当たる可能性がある。もしそうなら、テーマ選定そのものを見直す判断があり得る。**着工前に必ず答えてほしい。**

**なお、あくまで「入っているアドオンから推測しただけ」であって、決めつけではない。** 実際にどれをどれくらい使ってきたかは本人しか知らないので、Q15・Q16 は自分の言葉で書いてほしい。

**確定した好材料も1つある。** glTF エクスポータ（`io_scene_gltf2` 5.0.21）と FBX（`io_scene_fbx` 5.14.0）が有効なので、**Q21 の「glTF で出力」は追加の準備なしに実現できる。**

---

# Part 3. 一緒に決めたいこと

## Q17. リポジトリ名

現在未定。public化する前提なので、後から見て内容が分かる名前にしたい。

案:
- `multiview-visualhull-recon`
- `recon3d`（シンプル。Pythonパッケージ名もこれにする想定）
- `silhouette-sfm-fusion`

**【これが変わる】** パッケージ名（`src/recon3d/` の部分）も揃えるので、早めに決めたい。

```
回答:
```

---

## Q18. 合成データの元にする3Dモデル 【W01】

君が Blender で用意する最初のデータ。**条件が1つある。**

> **必ず「凹んだ形状」を含むモデルにすること。**

理由: このプロジェクトの最重要実験は「①のみ / ④のみ / 統合」の3条件比較で、**統合が優れていることを数字で証明する**もの。ところが凸形状（球、Suzanne など）だけだと、シルエット法が苦手とする凹みが存在しないので、統合の意味が数字に出ない。

**理想は「無地の面 + 凹み」の両方を持つもの。**

| 条件 | 失敗する手法 |
|---|---|
| 無地・模様がない面 | ① 多視点復元（特徴点が取れず点群に穴が空く） |
| 凹んだ形状 | ④ シルエット復元（原理的に削れない） |

**マグカップがこの両方を満たす**（陶器の無地の面＋内側の凹み）。だから主データはマグカップ相当を推したい。

副データとして Stanford Bunny のような凸・高テクスチャのモデルも1つ欲しい（①単独の上限性能を見るため）。

**【これが変わる】** 実験結果の説得力そのもの。

```
回答:
主データに使いたいモデル:
副データに使いたいモデル:
入手先（自作 / 既存アセット / 配布モデル）:
ライセンスは大丈夫そう？:
```

---

## Q19. 実物撮影の被写体

Phase 3（11月）から。Q18と同じ理由で、**凹みと無地面の両方**が欲しい。除外するのは鏡面（金属・光沢）と透明物（両手法とも原理的に無理）。

```
回答:
候補:
```

---

## Q20. メッシュ処理をどこまで自作するか

Q1の核2に関わる。君の判断を聞きたい。

| 処理 | 選択肢 |
|---|---|
| **Marching Cubes**（ボクセル→メッシュ） | 自作する / `skimage.measure.marching_cubes` を使う |
| **メッシュ簡略化** | 自作する / Open3D の機能を使う |
| **UV展開** | 自作する / Blender の Smart UV Project を使う |
| **テクスチャ投影** | 自作する（推奨。ここは君の見せ場） |

現在の計画では**Marching Cubes と メッシュ簡略化は自作、UV展開は Blender の機能を使う**想定にしている（UV展開は独立した研究テーマになるほど深く、片手間で作るものではないため）。

**トレードオフ**: 自作を増やすと「中身を理解している」と言えるが、工数を食う。Q6 の315時間には Marching Cubes 20h・UV/テクスチャ 25h が計上済み。

**【これが変わる】** 工数配分と、君が「これは自分が作った」と言える範囲。

```
回答:
```

---

## Q21. 最終的な出力の形

何をもって「完成」とするか。

```
回答（欲しいものにチェック）:
□ テクスチャ付きメッシュ（.obj + .png）
□ Blender シーンファイル（.blend）
□ glTF（Web で見られる形式）
□ デモ動画（回転しながら表示）
□ 復元過程の可視化動画（ボクセルが削られていく様子）
□ その他:

一番見栄えがすると思うもの:
```

---

## Q22. 撤退ラインの認識合わせ

野心的な構成なので、**先に「どこまで諦めるか」を決めてある。** 上から順に諦める。

| 段階 | 到達内容 | 位置づけ |
|---|---|---|
| **L4** | 統合＋実験＋ビューア＋ドキュメント一式 | 理想 |
| **L3** | 統合パイプライン完成、Blender出力、比較実験 | **目標** |
| **L2** | ④のみ（カメラ位置を回転台の角度から直接計算）でメッシュ生成 | 合格ライン |
| **L1** | ①のみ、点群生成＋収束実験 | 最低保証 |

**判定は2回。**

| 時期 | 判定内容 | 落ちた場合 |
|---|---|---|
| **W11 終了時（11/1）** | 最適化（バンドル調整）が収束しているか | → L2へ。カメラ姿勢推定を回避してメッシュに全振り |
| **W15 終了時（11/29）** | メッシュが出ているか | → L1へ。点群と数値実験に集中 |

**L2 に落ちた場合、君の担当（メッシュ側）が主軸になる。** L1 に落ちた場合は逆に関口側が主軸。どちらに倒れても成果物にはなる。

**【これが変わる】** ここを事前に共有しておかないと、判定のタイミングで「諦めるのか」という話になって揉める。**先に合意しておきたい。**

```
回答:
□ 了承。この判定基準とタイミングでいい
□ 気になる点がある:
```

---

## Q23. 週の進め方（曜日と時間帯）

```
回答:
作業しやすい曜日・時間帯:
週1回の対面ミーティングを固定するなら、いつがいい？:
連絡手段（Discord / LINE / Slack など）:
返信の速さの期待値（即レス / 1日以内 / 数日）:
```

---

## Q24. 逆に、関口に聞きたいこと・言っておきたいこと

**遠慮なく書いてほしい。** 特に以下は歓迎する。

- 計画の中で納得できない部分
- 「これは無理そう」と感じる部分
- 「これもやりたい」という追加提案
- 数理側の説明で分からなかった用語

```
回答:
```

---

# 回答後の流れ

1. このシートの回答をもとに、計画書を修正する ← Q9 の分は 2026-08-15 に反映済み
2. ~~Q9（Blenderバージョン）が分かり次第、Pythonバージョンを確定~~ → **✅ 完了。Blender 5.0.1 / Python 3.11.13 を確認し、プロジェクトの Python を 3.11 に確定した**
3. **W01（8/17の週）にペアプロ#1（2時間）** — 座標系の規約と `cameras.json` の仕様を2人で決めて実装する
4. それが終われば、以降は互いの完成を待たずに並行して進められる

## いま返事がほしいものだけ、もう一度まとめる

Blenderの情報、ありがとう。**これで Python のバージョンが確定して、関口側は W01 の環境構築に着手できる。**

残りで急ぐのはこの4つだけ。他は後でいい。

| 優先 | 設問 | なぜ急ぐか |
|---|---|---|
| 1 | **Q1・Q2・Q3** | Noなら計画を組み直すため。特に **Q2**（リアルタイム描画が出てこないこと）は、受領したアドオン構成を見て確認の必要性が上がった |
| 2 | **Q15**（志望研究室の分野） | 同上。アニメーション系ならテーマ選定から見直す判断があり得る |
| 3 | **Q13 のメモリ容量** | ボクセル解像度（256³ か 128³ か）を決められない |
| 4 | **Q17**（リポジトリ名）・**Q18**（合成データの元モデル） | W01 で必要 |

**あわせて1つお願い**: 開発期間中（〜2026-12）は **Blender を 5.0.x のままにしてほしい。** 5.0.x は LTS ではないので、上げると内蔵 Python が動いて、プロジェクト全体の Python を追随させる必要が出る。上げたい事情ができたら先に一報がほしい。

---

## 補足：出てきた用語

> **この節にあった用語表は [docs/glossary.md](../glossary.md) に移し、そちらで大幅に拡充しました（2026-08-24）。**
> **定義が2か所にあると必ず食い違うため、こちらの写しは削除しています。** 分からない言葉は [用語集](../glossary.md) を引いてください。
